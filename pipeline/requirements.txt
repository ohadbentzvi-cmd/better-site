# BetterSite pipeline — Python dependencies
# Pinned loosely; lock with pip-compile or uv.lock when repo matures.

# Core
python-dotenv>=1.0,<2.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
structlog>=24.1,<25.0
tenacity>=8.2,<9.0

# Database
sqlalchemy[asyncio]>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
alembic>=1.13,<2.0

# Orchestration
prefect>=3.4,<4.0

# HTTP
httpx>=0.27,<1.0

# Scraping + vision inputs
playwright>=1.43,<2.0
beautifulsoup4>=4.12,<5.0
lxml>=5.1,<6.0
colorthief>=0.2.1,<1.0
Pillow>=10.2,<11.0

# AI
anthropic>=0.25,<1.0

# Object storage (S3-compatible, works with Cloudflare R2)
boto3>=1.34,<2.0

# Utilities
nanoid>=2.0,<3.0
tldextract>=5.1,<6.0

# Observability
sentry-sdk>=1.45,<2.0

# Dev / test
pytest>=8.1,<9.0
pytest-asyncio>=0.23,<1.0
ruff>=0.3,<1.0
mypy>=1.9,<2.0
