{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'alembic -c pipeline/alembic.ini upgrade head && uvicorn api.main:app --host 0.0.0.0 --port $PORT'",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
